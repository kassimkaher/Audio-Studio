name: VoiceStudio
options:
  bundleIdPrefix: com.voicestudio
  deploymentTarget:
    iOS: "16.0"
    macOS: "13.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    DEVELOPMENT_TEAM: "46WJZZYGW4"
    CODE_SIGN_STYLE: Automatic
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    ENABLE_USER_SCRIPT_SANDBOXING: YES

targets:
  VoiceStudio:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: VoiceStudio
        excludes:
          - "Views/Mac"
          - "Resources/VoiceStudioMac.entitlements"
    info:
      path: VoiceStudio/Resources/Info.plist
      properties:
        CFBundleDisplayName: Voice Studio
        UILaunchScreen: {}
        NSMicrophoneUsageDescription: "Voice Studio needs microphone access to record your voice for editing, effects, and montage."
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voicestudio.app
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"

  VoiceStudioMac:
    type: application
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: VoiceStudio
        excludes:
          - "Views/iOS"
          - "Resources/Info.plist"
          - "Resources/VoiceStudioMac.entitlements"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voicestudio.mac
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: VoiceStudio/Resources/VoiceStudioMac.entitlements
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "Voice Studio needs microphone access to record."
        INFOPLIST_KEY_LSApplicationCategoryType: "public.app-category.music"
        INFOPLIST_KEY_NSPrincipalClass: "NSApplication"

  VoiceStudioTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: VoiceStudioTests
    dependencies:
      - target: VoiceStudio
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voicestudio.app.tests
        GENERATE_INFOPLIST_FILE: YES

  VoiceStudioUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: VoiceStudioUITests
    dependencies:
      - target: VoiceStudio
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voicestudio.app.uitests
        GENERATE_INFOPLIST_FILE: YES

  VoiceStudioMacTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: VoiceStudioTests
    dependencies:
      - target: VoiceStudioMac
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voicestudio.mac.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  VoiceStudio:
    build:
      targets:
        VoiceStudio: all
        VoiceStudioTests: [test]
        VoiceStudioUITests: [test]
    test:
      targets:
        - VoiceStudioTests
        - VoiceStudioUITests

  VoiceStudioMac:
    build:
      targets:
        VoiceStudioMac: all
        VoiceStudioMacTests: [test]
    test:
      targets:
        - VoiceStudioMacTests
